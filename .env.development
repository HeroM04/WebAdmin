# ==============================================================
# .env.development — dùng khi chạy `npm run dev` (local only)
# ==============================================================

VITE_API_BASE_URL=http://localhost:8088/api/v1
VITE_WS_URL=http://localhost:8088/ws
